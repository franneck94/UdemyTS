enum ColorChannels {
    RED = 0,
    GREEN = 1,
    BLUE = 2,
}

const redChannel = ColorChannels.RED;
console.log(redChannel);

enum PermissonLevel {
    USER = 'USER',
    INSTRUCTOR = 'INSTRUCTOR',
    ADMIN = 'ADMIN',
}

const jan = PermissonLevel.INSTRUCTOR;
console.log(jan);
